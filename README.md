# Modeling Hard Drive Failures

## Notes
all files are run explicitly with the python interpreter
(e.g. `python3 ./stack_tables.py` as opposed to `./stack_tables.py`)

Quarters are referred to *in reverse* since Q4 2025 (Q4 2025 is q1, Q3 2025 is q2, Q4 2024 is q5)
